# Audit Register — 2026-08-03

Immutable finding register from the full-project Unity/C# audit (8 dimensions, adversarial verification).
**82 findings survived verification, 12 claims were refuted and dropped. 0 critical.**

This file is the **source of truth for what is broken**. It is append-only for findings;
only the `St` (status) column changes as work lands. Never delete a row — mark it `done` or `wontfix`.

Companion docs:
- [`AUDIT_2026-08_PLAN.md`](AUDIT_2026-08_PLAN.md) — how to fix, grouped into stages.
- [`SESSION_LOG.md`](SESSION_LOG.md) — cross-session handoff state. **Read this first when resuming.**

---

## Conventions

**ID** — stable forever. Reference it in commit messages (`fix(async): main-thread contract [A-H01]`).

**Vd** (verdict from the verification pass):
- `C` — CONFIRMED: verifier read the code and the failure scenario provably follows.
- `P` — PLAUSIBLE: concern is real, but the concrete failure depends on data or a runtime path
  that could not be confirmed from source alone. **Reproduce before spending effort.**

**St** (status): `open` · `wip` · `done` · `wontfix` · `needs-repro`

**Stage** — target stage in `AUDIT_2026-08_PLAN.md`. `—` = unassigned / opportunistic.

---

## HIGH (12)

| ID | Vd | Location | Finding | Fix | Stage | St |
|----|----|----------|---------|-----|-------|----|
| A-H01 | C | `FileSystem/FcFileSystem.cs:144` | `ReadAllBytesAsync` has `SwitchToMainThread` only on the success path; `ReadAllBytes` throws `FileNotFoundException` at :140 **on the pool thread**, so every failure continuation downstream (`TextureRuntimeImportService.cs:322` → `CgfMaterialImportService` → `FcBrushLoadService.LoadOneAsync:130`, `FcMeshLoadService:226`) resumes off the main thread. A missing normal/spec map is the normal case, not an edge case. **Root cause of A-H02, A-H03, A-M01, A-M02.** | `try { ... } finally { await UniTask.SwitchToMainThread(); }`; move `ThrowIfCancellationRequested` before the `SwitchToThreadPool` hop. | 1 | done |
| A-H02 | C | `Level/Services/FcBrushLoadService.cs:170` | `finally` runs `_activeCount--` non-atomically from a pool thread (early returns at :124/:140, see A-H01) while `Update` does `_activeCount++` at :73. After 8 lost decrements the guard at :62 is permanently true — `Update` stops starting brushes, the level loads partially, `LogReport` at :175 never fires. Silent stall. Same shape in `FcVegetationLoadService.cs:167-170`. | `Interlocked.Decrement` + `await UniTask.SwitchToMainThread(CancellationToken.None)` immediately after the awaits at :124/:140. | 1 | done |
| A-H03 | C | `Level/Services/FcEntityLoadService.cs:409` | Success path is safe (`SwitchToMainThread` at :425) but cancel/failure/catch/finally (:446-478, :501-503) mutate `_stateByEntity` / `_active` / `_activeCount` from the pool while `DrainList` (:358-360) walks them. `HashSet` resize during `Remove` corrupts the bucket array. | `SwitchToMainThread` immediately after the awaits at :392 and :422. | 1 | done |
| A-H04 | C | `Level/Services/FcLevelCacheService.cs:17` | `OnDestroy` cancels only `FcEntityLoadService` (:21); brush/vegetation loaders have no scope-cancel and no drain. `ReleaseLevelScope` → `CgfFile.Dispose()` (`CgfRuntimeAssetCache.cs:231`) frees Persistent arrays while a pool thread reads `chunk.Vertices/UVs` in `CgfMeshBuilder.cs:409-433` — the owner branch passes `CancellationToken.None` (`CgfRuntimeImportService.cs:440`). Editor: `InvalidOperationException` off the main thread. Player: read of freed memory. Reverse order (`StoreModel` after release) leaks the Mesh + parsed entry for the session. | Linked "scope alive" token instead of `CancellationToken.None` at :368/:440; `ReleaseLevelScope` skips keys present in `_inFlightParsed`/`_inFlightModel`. | 6 | open |
| A-H05 | C | `FileSystem/PakArchive.cs:98-120` | `_zipFile` is published at :100 by a non-volatile read outside the lock, while `_index`/`_directoryIndex` are still being filled at :107-118. `Exists` (:34), `FileCount` (:14), `GetEntriesInDirectory` (:67) take no lock at all, and `FcFileSystem` holds only a **read** lock, so N threads are inside concurrently. Live window: lazy `level.pak` mount via `EnsureLevelMounted` while loads are already running. Symptoms: false "file missing" (brush/texture silently skipped), `IndexOutOfRangeException`, `Collection was modified`. | Build both indexes into locals inside the lock; publish `_zipFile` + indexes last via `Volatile.Write` behind an `_indexed` flag. Materialize `GetEntriesInDirectory` into a list under the lock. | 6 | open |
| A-H06 | C | `Level/Services/FcBrushLoadService.cs:47,65,84` | `Register` uses `List.Contains` → N²/2 comparisons in a single frame (N=10000 → 5·10⁷, hundreds of ms hitch). Then with `_loadsPerFrame=8` the list lives ~N/8 frames and every frame sorts the whole thing with a **capturing** comparator (2 allocs/frame) doing two `Transform.position` interop calls per comparison — for transforms the scene builder baked and that never move. `RemoveAt(i)` at :71 adds O(n). `FcVegetationLoadService` already solves this with a `HashSet` + `_distanceSortMaxPending` guard. | Snapshot positions into `Vector3[]` at Register; `HashSet` for dedup; cached static `Comparison<T>`; re-sort only past a camera-movement threshold (or partial-select nearest 8, O(N)). **Partially done**: mirrored exactly what `FcVegetationLoadService` already has — `HashSet<FcBrushInstance>` for O(1) `Register`/`Unregister` (kills the N² `List.Contains`), and a `_distanceSortMaxPending` guard skipping the sort above 5000 pending. The capturing-lambda / per-comparison `Transform.position` reads are **not** fixed (vegetation doesn't have that fix either — nothing to mirror); still 2 allocs + 2 Transform reads per comparison when the sort does run under the threshold. | 3 | done |
| A-H07 | C | `Level/Services/FcVegetationTerrainService.cs:540` | `_visibleCellScratch.Count == 0` is OR-ed with the legacy `_runtimeCells == null` guard. Turn the camera away from vegetation → full linear scan of ~50K instances **and** everything passing the LOD distance test is submitted to `DrawMeshInstanced` behind the camera. Looking away costs more than looking at it. `CollectColliderCandidates` (:601) repeats it 5×/s. | Split the conditions: legacy scan only when `_runtimeCells == null`; on an empty visible set just `return`. **Done** in both `CollectVisibleInstances` and `CollectColliderCandidates`. | 3 | done |
| A-H08 | C | `Level/Services/FcBrushGeometryPostProcessor.cs:99,320,330-340,476` | For CGF whose collision is a separate `*proxy*` node (not nodraw faces) `BuildResult.ColliderMesh` is empty, so `TryBuildFromProxyNodeMesh` → `BuildColliderMeshFromFaces` does `new Mesh` + a full CPU vertex transform **per instance**, plus a separate PhysX cook on `MeshCollider.sharedMesh` assignment. 2000 copies of a crate = 2000 meshes and 2000 cooks instead of one. | Cache `ApplyRuntimeParity` artifacts (physics mesh, filtered mesh) in the level scope keyed by model cache key, refcounted; `FcBrushInstance.OnDestroy` (:427-430) must not destroy shared meshes. | 7 | open |
| A-H09 | C | `Importer/Cgf/CgfMeshBuilder.cs:575-588` + `CgfMaterialBuilder.cs:113-118` | Vertex layout is Position/Normal/TexCoord0 only and `RecalculateTangents` is never called, yet materials enable `_NORMALMAP`. URP Lit reads `TANGENT`, gets a degenerate TBN → the normal map sample is unrelated to the surface. We pay decode, VRAM, an extra sample and a heavier shader variant for nothing. | Add `VertexAttribute.Tangent` to the layout and compute it in the existing job (stop-gap: `RecalculateTangents()` before :617). **Bump `MeshCacheVersionName` (:82).** Otherwise drop the `_NORMALMAP` block. | 5 | open |
| A-H10 | C | `Level/Editor/FcVegetationCatalogBuilder.cs:78` | `EditorSceneManager.NewScene(..., NewSceneMode.Single)` with no save prompt. The dialog at :34-39 only warns about overwriting `VegetationCatalog.unity`. `SaveCurrentModifiedScenesIfUserWantsTo` does not appear anywhere in the repo. No Undo. **Destroys unsaved scene work.** | `if (!EditorSceneManager.SaveCurrentModifiedScenesIfUserWantsTo()) return;` at the top of `BuildCatalogScene`, and fix the dialog text. **Done**: guard added right after the confirmation dialog; dialog text now names scene closure explicitly. | 2 | done |
| A-H11 | C | `Importer/Editor/CgfAssetCacheService.cs:97,118-119` + `FcLevelBuilderWindow.cs:1151-1159` | N cache misses = N full import-pipeline syncs plus N `Unwrapping.GenerateSecondaryUVSet` on a blocked main thread. `StartAssetEditing` exists in exactly one place in the repo (`FcEntityPrefabFactory.cs:28`). `DisplayProgressBar` instead of `DisplayCancelableProgressBar` (the repo has zero cancelable bars) — a build started on the wrong level can only be stopped by killing the Editor, leaving the FCData cache half-written. Paid on first build of any level and after every `MeshCacheVersionName` bump. | Wrap the :1148-1209 loop in `StartAssetEditing`/`StopAssetEditing`, drop per-item `SaveAssets`/`Refresh`, one `Refresh` after the loop, switch to a cancelable bar with a `foreach` break. **Done**: `EnsureCachedPrefabsForProfiles` and `PreBakeLevelMaterials` (same shape, same file) both wrapped in `StartAssetEditing`/`StopAssetEditing`, cancelable bar with `break` + warning log on cancel; the per-item flush inside `CgfAssetCacheService.SaveAssets` removed. Re-added the flush in `CgfImporterWindow.SaveAssets` (its one *non-batched* single-shot caller) so that tool still persists correctly. | 2 | done |
| A-H12 | **P** | `Level/Editor/FcLevelSceneBuilder.cs:1289` (also :1323, :1342) | `PrefabUtility.InstantiatePrefab` → `SetTransform` → `FcEntity.SetData` write serialized fields by plain C# assignment; `RecordPrefabInstancePropertyModifications` is absent from the repo entirely. Brushes (:263-268), vegetation (:395-399) and water (:1105-1107) go through `SerializedObject` — entities are the sole exception. On prefab re-merge the values may revert to defaults (entities collapse to origin, class/properties empty) with nothing in the log. Follows from the API contract, not from observation. | Reproduce first (build a level → reimport the prefab → inspect fields). Then `RecordPrefabInstancePropertyModifications(entity)` + `(go.transform)` after `SetData`, or use `SerializedObject` like brushes do. **Not done**: needs an interactive Editor repro (build level, reimport an entity prefab, inspect fields) no CLI session can drive. | 2 | needs-repro |

---

## MEDIUM (29 after merging duplicates)

| ID | Vd | Location | Finding | Fix | Stage | St |
|----|----|----------|---------|-----|-------|----|
| A-M01 | C | `Importer/Texture/TextureRuntimeImportService.cs:1024-1059` | `PreloadTexturesAsync` (`CgfMaterialImportService.cs:393-420`) fans out to `chunks.Count*5` tasks, each landing in `LogMissingPath` on its own pool thread. Unguarded `HashSet.Add` during resize → lost dedup (warning spam returns), lost counter increments, worst case a spin in `AddIfNotPresent` and a `WhenAll` that never completes. | Consequence of A-H01. Locally: `lock` + `Interlocked`. **Done**: single `_diagnosticsLock` guards both `HashSet`s, all 7 plain counters, the 2 format `Dictionary`s (incl. `GetRuntimeDiagnostics`' read), and `ClearRuntimeCache`. | 1 | done |
| A-M02 | C | `Importer/Cgf/CgfLodImportService.cs:20-30` | Plain `static Dictionary<string,Regex>` with unsynchronized insert; concurrent inserts from two pool threads (A-H01 again) return another model's Regex (a model gets a foreign LOD pattern) or spin in `TryInsert`. Also never cleared, and every entry is a `RegexOptions.Compiled` pattern whose IL is never unloaded — for patterns matched a few dozen times. | `ConcurrentDictionary.GetOrAdd`, drop `Compiled`, clear on level unload. **Done**: `ConcurrentDictionary` + `GetOrAdd`, `Compiled` dropped from the per-basename entries (kept on the 2 fixed static patterns), `ClearSiblingRegexCache()` wired into `CgfRuntimeImporter.TrimUnused()`. | 1 | done |
| A-M03 | C | `Level/Services/FcLevelLoadService.cs:86` | `IsLoadInProgress` set without `try/finally`. A missing `mission_<name>.xml` (`FcLevelLoader.cs:74` throws) or cancellation at :100/:105/:110 leaves the flag `true` forever. `FcVegetationTerrainService.cs:208-213` then parks on `WaitUntil` for the whole session and never reaches `BuildRuntimeTypes` — level with brushes and zero vegetation, no extra error. | `try/finally`, reset the flag in `finally`, keep `IsPreloadComplete` on success only. **Done**: body split into `LoadLevelInnerAsync`, outer wrapper resets the flag in `finally`; original early success-path reset left in place (redundant, harmless). | 1 | done |
| A-M04 | C | `FileSystem/PakArchive.cs:57-63` | The read loop exits on `read == 0`; `offset` is never compared to `data.Length` and SharpZipLib does not verify CRC. Tail truncation zero-fills the chunk table → `CgfParser.ReadChunkTable` (:153-200) reads `count = 0`, passes every bounds check and returns an empty `ChunkHeader[]`: the brush becomes an empty GameObject **with no message at all**. | `if (offset != data.Length) { Debug.LogError($"[PakArchive] Truncated entry '{key}': {offset}/{data.Length}"); data = null; return false; }`. | 6 | open |
| A-M05 | C | `Importer/Cgf/CgfParser.cs:229-252, 73-129` | `ValidateCount` checks only `MaxReasonableEntities`, never `h.SizeBytes`: `nVerts * sizeof(CryVertex)` is allocated before `ReadBytesPtr` throws `IndexOutOfRangeException`, leaking Vertices/Faces/UVs/TexFaces one at a time. Separately the rethrow at :126-127 drops `file` without `CgfFile.Dispose`, losing every already-parsed mesh chunk. The realistic trigger is not a corrupt file but an unhandled chunk variant in `ReadNode`/`ReadBoneAnim`/`ReadMaterialChunk`. Failures are not negatively cached → the leak repeats per attempt. | Validate `(long)count * stride` against the remaining chunk window; `try/catch` + Dispose in `ReadMesh`; wrap both `Parse` loops so `file.Dispose()` runs before the exception escapes. | 6 | open |
| A-M06 | C | `Importer/Cgf/CafParser.cs:205-216` (and :260-270) | Same shape: `numKeys` (up to 32768) is not checked against `h.SizeBytes`; ticks/positions/rotations (Persistent) + rawRotLogs (TempJob) are allocated before reading. `CafLoader` caches nothing on failure and `CgfAnimationRuntimeImportService` logs and continues — every subsequent character using that `.cal` re-parses and leaks again. Up to ~1 MB Persistent per attempt. | Validate `(long)numKeys * bytesPerKey <= h.SizeBytes - r.Offset`; `try/catch` + Dispose + rethrow; wrap the chunk loop in `CafParser.Parse` (:78-93). | 6 | open |
| A-M07 | **P** | `Importer/Cgf/CgfRuntimeAssetCache.cs:124-129` | Synchronous `Import` (`FcLevelResourceService.cs:38`, `FcVegetationTerrainService.cs:1026`) and the coalesced parse can diverge across await boundaries for the same key; the losing `CgfFile` with all its Persistent arrays becomes unreachable. Not proven that the same key is actually requested by both paths — content-dependent. | `StoreParsed` returns the canonical `CgfFile` (or a bool) so the caller disposes the loser, mirroring `TextureRuntimeScopedCache.Store`. | 6 | needs-repro |
| A-M08 | C | `Importer/Cgf/CgfRuntimeImportService.cs:404-406` (and sync `Import` :77-83) | `MeshBuildData.Dispose` is called in exactly one place — the end of `UploadPrepared` (`CgfMeshBuilder.cs:173`). Cancellation between `PrepareBuild` and `UploadPrepared` loses ~640 KB Persistent for a 20k-vertex mesh; the leak detector reports on the next domain reload. | Make `PreparedBuild` `IDisposable` / wrap in `try/finally`; move the `Dispose` at `CgfMeshBuilder.cs:173` into a `finally`. | 6 | open |
| A-M09 | **P** | `Importer/Cgf/CgfAnimationSetCache.cs:451` | `CachedClipEntry` (:260-264) stores neither refcount nor scope; above 4096 live keys `EvictOldestClipEntriesUnsafe` calls `DestroyUnityObject` on a clip that `CgfAnimationRuntimeImportService.cs:364/371` already handed to a live component — the character freezes and `HasInvalidClipReference` then forces a full set re-decode. The 4096 threshold needs many distinct rigs; not verified against real data. | Non-destructive eviction (drop from the dictionary only); leave `Destroy` to `Clear()` / scope release. Or add refcount/scope tracking like `CgfRuntimeAssetCache`. | 6 | needs-repro |
| A-M10 | C | `Level/Services/FcBrushGeometryPostProcessor.cs:246,268` | `BuildCompactedMesh` for planar-decal submeshes reads `source.vertices/normals/uv` — full managed copies per instance — and the result is assigned to `sharedMesh`, which stops being shared: static batching and GPU instancing are lost. | Cache the compacted mesh by (model cache key + strip signature) in the level scope; release on scope release, not per-instance `OnDestroy`. Related: A-H08, A-M11. | 7 | open |
| A-M11 | **P** | `Level/Services/FcBrushGeometryPostProcessor.cs:327` | Every brush instance builds its own private copy of the shared cached mesh and collider mesh. | Cache the derived collider mesh per (virtualPath, CacheFormatVersion) next to `CgfRuntimeImportResult`, exactly as `BuildResult.ColliderMesh` is already shared. Related: A-H08. | 7 | open |
| A-M12 | C | `Level/Entities/FcMeshEntity.cs:188` + `FcLevelGeometryImportHelper.cs:43` | Per **brush**: `FindSiblingLodPaths` → `FcFileSystem.GetEntries` (allocates a `HashSet<string>` of the whole subtree) → `PakArchive.GetEntriesInDirectory` (`PakArchive.cs:67-84`) which — despite Phase 1 being marked done in `BUILD_SCENE_PERF_PLAN.md` — still walks **every** `_directoryIndex` key with `StartsWith`. Plus a LINQ tail (`OrderBy/ThenBy/Select/ToList`) per call. M models = M full scans across all archives. | Static memo in `CgfLodImportService`: `dir → string[]` and `modelPath → List<string>`, invalidated on mount change. Make `GetEntriesInDirectory` an O(1) non-recursive lookup. `List.Sort` with a cached comparer. | 7 | open |
| A-M13 | C | `Importer/Cgf/CgfUvScrollRuntime.cs:20,29,37` | `Update` calls `GetComponentsInChildren<Renderer>(true)` (the allocating overload) plus a full hierarchy walk, forever, while the loop body is a no-op after the first pass. Also `mat.HasProperty("_BaseMap")` resolves a string against the material every frame. The rescan is load-bearing (LOD children are added later, `FcBrushInstance.cs:90`) so it cannot simply be deleted. | Public `Refresh()` called from `Awake` and after `FcLevelRuntimeLodGroupBuilder.Apply` / `ConfigureLodGroup`; `Shader.PropertyToID` into `static readonly`; `HasProperty` once at material clone time (:61). **Done**: `Update` no longer scans; `Refresh()` called from `Awake` plus hooked into both LOD assembly paths (`CgfLodImportService.ConfigureLodGroup` and `FcLevelRuntimeLodGroupBuilder.Apply` — the latter wasn't named in the finding but is the actual runtime-brush LOD path and had the identical gap). `_BaseMap` resolved via cached `Shader.PropertyToID`; materials without it are cloned (for renderer slot integrity) but excluded from the animated list at clone time instead of re-checked every frame. | 3 | done |
| A-M14 | C | `Level/Services/FcVegetationTerrainService.cs:575` | `_positions`/`_scales` are immutable after `BuildRuntimeInstances` (:329-360) and rotation is literal identity, yet `Matrix4x4.TRS` runs per visible instance per frame. V=30000 → 30000 managed→native transitions/frame (~1 ms) producing identical matrices. | `Matrix4x4[] _instanceMatrices` built alongside `_positions` (`Array.Resize` at :357-359); :575 becomes an array read. 64 B/instance ≈ 3.2 MB at 50K. **Done** exactly as specified. | 3 | done |
| A-M15 | C | `Level/Services/FcEntityLoadService.cs:329` | Sorts and rescans the entire deferred queue every frame with a capturing comparator. Same shape as A-H06. | Cache the entity world position into `QueuedEntityLoad` at Enqueue; cached `Comparison<T>`; re-sort only past a camera-movement threshold. **Partially done**: `WorldPosition` cached at `Enqueue` (and copied through on retry in `TryEnqueueRetry`), used by both `UpdateDeferredPromotions`' distance test and `SortList`'s sort key — removes the `Transform.position` interop calls. The comparator is still a per-call capturing lambda and there's no movement-threshold resort guard (entity counts per level are much smaller than vegetation/brush counts, so this was judged lower-priority than the position-read fix). | 3 | done |
| A-M16 | C | `Level/Services/FcVegetationTerrainService.cs:517,529,248-256` + `DrawBucketInstanced` :1463-1471 | Vegetation is frustum-culled against `Camera.main` only, then submitted to every camera with a `null` camera argument. The water planar reflection renders in the same frame (`FcPlanarReflectionRendererFeature.cs:117-141`) and consumes a set culled against the main camera: vegetation behind the player is missing from the reflection, vegetation outside the mirror frustum is drawn for nothing. A scene without a MainCamera-tagged camera loses all instanced vegetation. | Build the visible set per camera inside `beginCameraRendering` (cache keyed by `Camera`, as `FcReflectionCache` already does) and pass that camera to `DrawMeshInstanced`. **Done, without a Dictionary cache**: subscribed `OnBeginCameraRendering` in `OnEnable`/unsubscribed in `OnDisable`; cull+submit moved there, run once per camera (Game, Scene View, and now the water mirror camera too — `CameraType.Preview` excluded). No per-camera state needs to persist between calls (scratch buffers are just rebuilt fresh every invocation, and camera callbacks fire synchronously/non-interleaved), so `FcReflectionCache`'s `Dictionary<Camera,...>` pattern wasn't needed — just the subscription pattern. `Update()` keeps only the `Camera.main`-driven collider-proximity logic, which is a physics/gameplay concern, not a render-camera one. `DrawMeshInstanced` now receives `camera: cam` instead of `null`. | 3 | done |
| A-M17 | C | `Rendering/Water/FcPlanarReflectionRendererFeature.cs:164,189` | The guard compares `view.RtHDR == hdr` (requested flag) but stores `hdrOk` (resolved) → on a GPU without `RGB111110Float` the RT is Released+Destroyed+Created every frame per camera. Independently: `EnsureRT` never resets `HasRendered`, and the temporal-reuse early-out (:97-105) sits **after** `EnsureRT` — any legitimate reallocation (tier/resolution/aspect change) with a stationary camera binds a freshly created, never-rendered RT into `_FcWaterReflectionTex`. | `view.RtHDR = hdr;` at :189 and `view.HasRendered = false` in the reallocation branch. | 4 | open |
| A-M18 | C | `Rendering/Water/FcPlanarReflectionRendererFeature.cs:99-105` | The reuse predicate depends only on `worldToCameraMatrix`/`projectionMatrix` (eps 1e-4). Player stands still on the shore → `SubmitRenderRequest` is never called again: NPCs, foliage, vehicles and time-of-day are frozen in the reflection and snap when the camera moves. `TemporalReuse` defaults to on (`FcWaterSettings.cs:27`). | Staleness budget — re-render after N=4-6 frames or a `Time.time` threshold. | 4 | open |
| A-M19 | C | `Rendering/Water/FcPlanarReflectionRendererFeature.cs:74-75` | `_cache.CollectGarbage()` sits **after** eight early returns (:49-70). Level A with water → level B without: `FcWaterRegistry.Count == 0` returns at :52 every frame and the `HideAndDontSave` camera plus its RT (~1.5 MB VRAM) survive until domain reload. Same when the camera is underwater or the water is out of frustum. | Move `CollectGarbage()` above all guards, right after the `srcCam` checks; `ReleaseAll()` when `Count == 0`. | 4 | open |
| A-M20 | C | `Rendering/Water/FcPlanarReflectionRendererFeature.cs:18` | All work happens in the `RenderPipelineManager.beginCameraRendering` delegate subscribed from `Create()`; `AddRenderPasses` is empty. URP only honours `isActive` around `AddRenderPasses`, so unticking the feature in `PC_Renderer.asset` does **not** remove a second full scene pass (`ReflectionLayers = ~0`, occlusion culling off). | Early-return from `OnBeginCameraRendering` when `!isActive`. | 4 | open |
| A-M21 | C | `Rendering/Water/FcPlanarReflectionRendererFeature.cs:131` | Shipped `FcWaterSettings.asset:25` has `ReflectShadows: 1`, but the asset has no `ReflectionMaxShadowDistance` key → the C# initializer leaves it `0f`. `urp.shadowDistance = 0` makes URP disable main-light shadows entirely: reflections never have shadows and the toggle silently does nothing. | Treat `<= 0` as "inherit the source camera's shadow distance"; `renderShadows = ReflectShadows && effectiveDistance > 0`. | 4 | open |
| A-M22 | C | `Rendering/Water/FcSsprRendererFeature.cs:108-109` | `result` is never bound as a render target (only `SetComputeTextureParam`), and `autoGenerateMips` only fires on render-target unbind. `FarCryWater.shader:274-276` samples an explicit LOD of `_Roughness*6.0` (material `_Roughness 0.4` → LOD 2.4) — i.e. uninitialized RenderGraph pool memory. Currently unreachable only because the feature is not registered in `PC_Renderer.asset`. | Drop `useMipMap`/`autoGenerateMips`; clamp the sample to LOD 0 for the SSPR source (keyword or global mip cap — the LOD path is shared with the mirror-cam RT where mips do work). | 4 | open |
| A-M23 | **P** | `Level/Services/FcVegetationTerrainService.cs:1389-1427` | `BuildProxyMaterialIds` uses the **local** child index from `MaterialChildrenByParentChunkID`, while `CgfNoDrawFaceClassifier.cs:23-32` and `SubmeshMaterialIds` (`CgfMeshBuilder.cs:609`) use the **global** leaf index. With more than one MULTI chunk a false match strips a real visible foliage submesh (:1256-1259). `IsNoDrawProxyMaterial` also does not know `obstruct`, which `CgfMaterialClassifier.NameMarksCollisionOnly` handles. The "+1" retry at :1302-1306 is a workaround for exactly this. With a single MULTI chunk the indices coincide, so today the filter is a no-op — needs multi-MULTI content to reproduce. | Use `CgfNoDrawFaceClassifier.Classify`, or simply consume `result.BuildResult.ColliderMesh` which the vegetation service currently ignores, and delete the "+1". | 9 | needs-repro |
| A-M24 | C | `Level/Data/FcLevelLoader.cs:367` | No regression test pins `ApplyBrushMatrix34`/`ApplyEntityTransform` — the basis bridge CLAUDE.md explicitly requires to be validated as a whole. Swapping `-sZ` for `sZ` mirrors the entire level, colliders move with the visuals, suite stays green. | `FcLevelTransformRegressionTests.cs`: for a Cry `Matrix34` with identity rotation assert `t.position == (m03, m23, m13)` and `t.localScale.z < 0`; for a 90° rotation about Cry Z assert the resulting Unity rotation. | 8 | open |
| A-M25 | C | `Level/Services/FcBrushGeometryPostProcessor.cs:188` | Proxy/no-draw stripping — the "1-based fallback is collider-only" invariant CLAUDE.md flags as fragile — has no test at all. A regression produces invisible walls. | `FcBrushGeometryPostProcessorTests.cs`: synthetic `CgfFile` with three MatIDs (one proxy/nodraw-named) + a matching 3-submesh Mesh + `BuildResult.SubmeshMaterialIds`; assert exactly the proxy submesh is stripped from the visual and kept for the collider. | 8 | open |
| A-M26 | C | `Importer/Tests/Editor/FcKnownAssetRegressionTests.cs:14` | The only coverage of the 927-line `CgfParser` `Assert.Ignore`s itself without `~/Documents/farcry-game`. On a clean clone or an agent machine: zero executed parser coverage, exit code 0. `.github` does not exist — no CI. | In-memory `byte[]` fixtures (valid header + MESH/NODE/MTL chunks, plus truncated-chunk, bad-magic and offset-past-EOF cases). Keep the known-asset test as an optional extra. | 8 | open |
| A-M27 | C | `Level/Tests/Editor/OpenFarCry.Level.Tests.asmdef:4` | No reference to `OpenFarCry.Level.Editor`, so three test files drive editor logic through `Type.GetType`/`GetMethod`/`Invoke`: a signature change compiles clean and only fails at run time. | Add `OpenFarCry.Level.Editor` (and `OpenFarCry.FileSystem`) to `references`, promote the three reflected methods to `internal`, add `[assembly: InternalsVisibleTo("OpenFarCry.Level.Tests")]`, delete the reflection. | 8 | open |
| A-M28 | C | `Importer/Cgf/CgfRuntimeLoadSmokeTest.cs:8` | Zero PlayMode tests exist (both test asmdefs pin `includePlatforms: ["Editor"]`); the runtime scoped-release / UniTask-cancel path is verified by a hand-driven MonoBehaviour which — 301 lines, no `#if UNITY_EDITOR` — also compiles into the player. | PlayMode test assembly (`includePlatforms` empty, `optionalUnityReferences: ["TestAssemblies"]`): load a scope with a stub resource service, assert `ParsedEntryCount`/`ModelEntryCount > 0`, `ReleaseLevelScope` + `TrimUnused`, assert counts return to zero. Move the smoke test into an Editor assembly. | 8 | open |
| A-M29 | C | *(repo hygiene — found outside the code audit)* | `Assets/Plugins/Sirenix` — **93 files including DLLs, committed** to `github.com/VladimirDivak/open-fc`. Repo confirmed public — redistributed a paid asset against the Sirenix licence. `AmplifyImpostors` (112 files), `BOXOPHOBIC` (2116), `BOXOPHOBIC+` (6) were untracked **and absent from `.gitignore`** — one `git add -A` away from the same problem. | Ignore all four vendor dirs; purge Sirenix from history (`git-filter-repo`), force-push. **Done 2026-08-03**: backup mirror at `~/open-fc-backup-mirror-2026-08-03.git`; history rewritten on the only public ref (`refactor/level-builder-fcdata-cache`, root `834fa59`→`822475c`, 94 commits, repo 12M→7M); force-pushed; local branch reset+stash-pop verified byte-identical working tree; `.gitignore` updated, commit `pushed`. Local-only branches (`main`, `level-transform-fixes`, `level-vegetation-lod`) were never on origin and still contain the old Sirenix blobs in local history — harmless unless pushed; purge them the same way before ever pushing. | 0 | done |

---

## LOW (38)

Kept for completeness. Most are one-line fixes worth doing while already inside the file.
Do not schedule these on their own unless the parent stage touches the same code.

### Correctness / robustness

| ID | Vd | Location | Finding | Stage | St |
|----|----|----------|---------|-------|----|
| A-L01 | C | `Level/Data/FcTerrainLayerMaskDecoder.cs:41` | Allocates up to ~2.1 GB from a 4-byte header field; the catch-all then hides the failure. Bound `res` (`> 8192 → false`) and narrow the catch. | 6 | open |
| A-L02 | C | `Level/Data/FcBrushLoader.cs:61` | Three unvalidated file-supplied counts used directly as allocation sizes. Bound each against the remaining stream using the declared struct sizes. | 6 | open |
| A-L03 | C | `Importer/Texture/DdsRuntimeDecoder.cs:1032` | `mipCount` used unvalidated as a loop bound in `GetCompressedPayloadLength`. Clamp at parse time (:555) to `1..1+log2(max(w,h))`. | 6 | open |
| A-L04 | P | `FileSystem/PakArchive.cs:54` | Allocates from the ZIP-declared uncompressed size before reading a byte (zip-bomb shape). Reject implausible `Size`/`CompressedSize` ratios and log the path. | 6 | open |
| A-L05 | C | `Level/Data/FcLevelLoader.cs:722` | The iso-8859-1 fallback is dead code and a UTF-8 BOM makes the document fail to parse. Load from a `MemoryStream` so the reader detects the BOM. | 6 | open |
| A-L06 | P | `Level/Data/FcLevelLoader.cs:726` | `XmlDocument.LoadXml` on PAK-sourced XML with default DTD processing. Set `DtdProcessing.Prohibit` + `XmlResolver = null` in both copies. | 6 | open |
| A-L07 | P | `Level/Data/FcLevelSupplementLoader.cs:320` | `TraverseMaterialNode`/`FlattenProperties` recurse on XML nesting depth with no cap. Add a depth guard (`> 64 → return`). | 6 | open |
| A-L08 | C | `Importer/Cgf/CgfRuntimeAssetCache.cs:332` | `DisposeParsedNativeData` drops the model cache without destroying meshes, so a following `ClearRuntimeCache` is a no-op. Swap the order in `FcVegetationCatalogBuilder.ClearRuntimeCaches`. | 6 | open |
| A-L09 | P | `Importer/Cgf/CgfMaterialRuntimeCache.cs:64` | `ReleaseLevelScope` decrements `RefCount` for entries that never belonged to the scope. Add `if (!entry.Scopes.Remove(scopeId)) continue;` here and in `CgfScopedTextureCache`. | 6 | open |
| A-L10 | P | `Level/Services/FcMeshLoadService.cs:156` | Coalescing hands N callers one cache retain, so N releases underflow the refcount. Retain per consumer. | 6 | open |
| A-L11 | C | `Importer/Cgf/CgfRuntimeImportService.cs:350` | Parse/build coalescing ignores the joiner's `CancellationToken` while waiting. Use `AttachExternalCancellation(ct)` at :350 and :465, matching `FcMeshLoadService.AwaitInFlightAsync:166`. **Done**: both sites now `await tcs.Task.AttachExternalCancellation(ct)`, redundant explicit `ThrowIfCancellationRequested` calls removed. | 1 | done |
| A-L12 | P | `Level/Data/FcTerrainHeightmapDecoder.cs:68` | The extra (+1) heightmap row and column are never written. Clamp-extend the last row/column after the main loop. | 9 | open |

### Performance

| ID | Vd | Location | Finding | Stage | St |
|----|----|----------|---------|-------|----|
| A-L13 | C | `Level/Services/FcVegetationTerrainService.cs:517` | Allocating `GeometryUtility.CalculateFrustumPlanes` overload every frame. Use a cached `Plane[6]` field. | 3 | done |
| A-L14 | C | `Level/Services/FcVegetationTerrainService.cs:981` | Matrix bucket copied into `_batchBuf` once per submesh instead of once per 1023-instance page. Restructure so the page fill happens once. | 3 | done |
| A-L15 | C | `Level/Services/FcVegetationColliderSelector.cs:31` | Allocates a `HashSet` and a `Dictionary` per collider tick. Take a caller-owned destination set and `Clear()` it. **Done**: added a `destination`-taking overload (the old allocating 3-arg overload kept, delegating to it, so the existing test suite didn't need touching); `Dictionary` replaced with a reused `static readonly` scratch field cleared per call. | 3 | done |
| A-L16 | P | `Level/Services/FcBrushGeometryPostProcessor.cs:615` | `TryBuildDecalQuad` builds its unique-index set with `List.Contains`. Use a `HashSet<int>` and bail early on `tris.Length > 64`. | 7 | open |
| A-L17 | P | `Level/Editor/FcLevelBuilderWindow.cs:1896` | Per-material/per-texture `AssetDatabase.CreateAsset` inside the brush and vegetation build loops without `StartAssetEditing`. Wrap the persist loops and hoist `SaveAssets`/`Refresh`. **Done**: the actual calling loop (`CacheSceneGeometryToProject`'s `AttachCachedPrefabsToScene` block, which is what triggers the per-instance `PersistMaterialAsset`/`PersistTextureAsset` calls at :1896/:1953) wrapped in `StartAssetEditing`/`StopAssetEditing`; the leaf persist helpers themselves untouched. | 2 | done |
| A-L18 | P | `Level/Editor/FcLevelSceneBuilder.cs:486` | Vegetation instance array populated one `SerializedProperty` element at a time (4N calls) and stored inline in the scene. Add an internal setter and assign the prebuilt array once + `SetDirty`. **Done**: added `FcVegetationTerrainService.SetAuthoringData(types, instances)` (public, not internal — matches the existing `Initialize()`-method convention used by brushes/entities) covering both `_vegetationTypes` and `_instances` in one call; `FcLevelSceneBuilder` calls it once + `EditorUtility.SetDirty`. | 2 | done |

### Rendering

| ID | Vd | Location | Finding | Stage | St |
|----|----|----------|---------|-------|----|
| A-L19 | C | `Rendering/Water/FcWaterQualityTier.cs:54` | Disabling refraction clamps the whole water tier to `Off`. Make the toggles independent — gate only the refraction keyword. | 4 | open |
| A-L20 | P | `Rendering/Water/FcWaterRuntimeBootstrap.cs:32` | The `Sspr` tier enables the sampling keyword with no registered producer. Inspect the active URP asset's renderer for the feature the resolved tier needs; `LogError` + degrade. | 4 | open |
| A-L21 | P | `Rendering/Water/FcSsprRendererFeature.cs:62` | No compute-support, kernel or resource validation. Gate on `SystemInfo.supportsComputeShaders` + `HasKernel` for all three kernels. | 4 | open |
| A-L22 | P | `Shaders/Water/FcSsprCompute.compute:47` | Reversed-Z is hard-coded. Wrap sky rejection and the depth key in `#if UNITY_REVERSED_Z`. | 4 | open |
| A-L23 | C | `Shaders/Water/FarCryWater.shader:90` | Dead `_MAIN_LIGHT_SHADOWS` `multi_compile`. Delete it, or wire `TransformWorldToShadowCoord` + `GetMainLight(shadowCoord)` into the GGX term. | 4 | open |
| A-L24 | P | `Rendering/Water/FcReflectionCache.cs:33` | `DestroyImmediate` of GameObjects/RenderTextures from inside a render pipeline callback. Queue dead views and drain from `endContextRendering` / `delayCall`. | 4 | open |
| A-L25 | C | `Level/Editor/FcLevelSceneBuilder.cs:1095` | The procedural water grid mesh is never persisted, so it is serialized inline into every level `.unity` scene. Persist to `Assets/FCData/Levels/<level>/WaterGrid.asset` and reuse when size and cell count match. | 5 | open |

### Architecture / duplication

| ID | Vd | Location | Finding | Stage | St |
|----|----|----------|---------|-------|----|
| A-L26 | C | `Importer/IResourceImportService.cs:7` | Two dead members and two byte-loading methods that are aliases. Collapse to `IsSupportedVirtualPath` + `SupportedExtensions` + one `LoadResourceBytes`. | 9 | open |
| A-L27 | C | `Importer/Texture/TextureImportService.cs:7` | `SupportedExtensions` duplicates `TextureResourceImportService.Extensions`. Delete one and forward. | 9 | open |
| A-L28 | C | `Level/Editor/FcLevelSceneBuilder.cs:1219` | `EnsureDir` / `NormalizeLevelAssetKey` / the FCData level-cache path are duplicated between the writer and the deleter. Hoist to one `FcLevelAssetPaths`. | 9 | open |
| A-L29 | P | `Level/Services/FcLevelMaterialOverrideService.cs:556` | A parallel texture-resolution pipeline duplicating the importer's. Route through `CgfTexturePathResolver.NormalizeTextureName` (which additionally strips NUL and rebases roots). | 9 | open |
| A-L30 | P | `Level/Services/FcLevelMaterialOverrideService.cs:65` | Eight MonoBehaviour services use a static `Current` locator. Blocks additive level loading and tests. Resolve via `GetComponentInParent` from the level root, as `FcLevelLoadService.cs:46-48` already does. | 9 | open |
| A-L31 | P | `Level/Editor/FcLevelSceneBuilder.cs:25` | 1709-line static class mixing scene assembly, terrain, water and volume parsing. Extract the terrain block (:502-~1080) into `FcTerrainSceneBuilder`. | 9 | open |
| A-L32 | C | `Level/Editor/FcLevelBuilderWindow.cs:22` | 3442-line EditorWindow whose internals are bound by string reflection from tests. Minimal fix is the asmdef reference in A-M27. | 8 | open |
| A-L33 | P | `Level/Data/FcLevelLayoutDataV2.cs:520` | ~1160 lines of editor-only audit code inside the runtime assembly. Move `BuildImportReport`/`BuildValidation` into `OpenFarCry.Level.Editor`. | 9 | open |
| A-L34 | C | `Importer/Cgf/CgfSourceBrowser.cs:9` | Editor-only browser helper (203 lines) lives in the runtime Importer assembly. Move to `Importer/Editor/` after adding the asmdef reference from the tests. | 9 | open |

### Tests

| ID | Vd | Location | Finding | Stage | St |
|----|----|----------|---------|-------|----|
| A-L35 | C | `Importer/Tests/Editor/TextureRuntimeCacheTests.cs:54` | Asserts refcounts but never the destruction the caches exist to guarantee. Add `Assert.That(tex == null)` (Unity fake-null) after `TrimUnused`. | 8 | open |
| A-L36 | P | `Importer/Tests/Editor/CgfMaterialTextureBindingTests.cs:12` | The largest test file is one `Shader.Find` away from being entirely skipped, with no non-ignorable guard. Add `UrpLitShaderIsAvailable`. | 8 | open |
| A-L37 | P | `Importer/Cgf/CgfMeshJobs.cs:38` | `CryTransformConversion` has no direct tests and its math is hand-duplicated into Burst jobs. Use a non-zero vertex in at least one static-mesh test to pin the inlined swizzle. | 8 | open |
| A-L38 | — | *(see A-M28)* | No PlayMode assembly at all. Tracked under A-M28. | 8 | open |

---

## Refuted claims (12) — do not re-file

The verification pass read the code and rejected these. If a future audit raises them again, read
this table first — the refutation names the guard that already exists.

| Claim | Location | Why refuted |
|-------|----------|-------------|
| Heightmap resolution from level XML overflows the h16 size check; uncaught `OverflowException` aborts the editor build and leaves a half-built scene | `Level/Editor/FcLevelSceneBuilder.cs:523` | Arithmetic is real (`HeightmapSize=46341` wraps to `new ushort[-2147479015]`), but both call sites wrap `BuildUnityTerrain` in `try/catch` (:82-95, :202-216) logging "Terrain build failed, continuing without terrain", and nothing is written to `Assets/FCData/Levels` before :523 — persistence starts at :537. Hardening nit, not a defect. |
| `OpenFarCry.Rendering.Water` has no test assembly | `Rendering/Water/FcReflectionCache.cs:55` | Absence of tests is factual, but the concrete failure scenario did not survive reading. Tracked implicitly by Stage 8 instead. |
| Cache-owned `NativeArray`s exposed as public mutable fields with no ownership contract | `Importer/Cgf/CgfData.cs:185` | Structural facts hold, but every Level-side consumer is guarded; the scope-release policy is explicitly documented at `CgfRuntimeAssetCache.cs:207-210`. |
| Runtime materials shared by name across brushes/vegetation/entities, then mutated globally | `Importer/Cgf/CgfMaterialImportService.cs:558` | Premise wrong. The cache key is `sourceVirtualPath + '#' + chunk.ChunkID + '#' + overrideName` (:558-565), not the material name. |
| `PlaceVegetationAuthoringWrappersInExistingScene` mutates an open scene without `MarkSceneDirty` or Undo | `Level/Editor/FcLevelSceneBuilder.cs:147` | The sole caller runs `ClearScene` + `BuildScene` immediately before, and `BuildScene` ends with `MarkSceneDirty` (:140) — the scene is already dirty. Missing Undo is by design; teardown is the "Strip APV Cached Geometry" button. |
| `CgfRuntimeImportService` leaks Persistent arrays when a no-cache import is cancelled after `PrepareBuild` | `Importer/Cgf/CgfRuntimeImportService.cs:396` | Mechanism correct but the branch is unreachable: every `ImportAsync` caller passes `useRuntimeMemoryCache: true`. (The reachable variant of this leak **is** filed — see `A-M08`.) |
| `FcLevelMaterialOverrideService.Clear()` wipes lookups while brush loads read them off-thread | `Level/Services/FcLevelMaterialOverrideService.cs:139` | Trigger does not exist: `Clear()` is called only from `FcLevelLoadService.UnloadLevelAsync`, which has zero callers anywhere. |
| Play Mode writes runtime-created textures/layers into the persistent `TerrainData` and terrain `.mat` | `Level/Services/FcTerrainTextureService.cs:122` | Mutation quoted correctly, but disqualified by the same reasoning as the row below. |
| `FcTerrainTextureService` never destroys its `Texture2DArray` / transposed cover texture / `TerrainLayer`s | `Level/Services/FcTerrainTextureService.cs:183` | Code facts accurate (no `OnDestroy`), but the concrete failure did not survive verification. |
| Persisted override-texture asset paths embed `GetInstanceID()`, so every rebuild session orphans copies | `Level/Editor/FcLevelBuilderWindow.cs:1919` | The path does embed a session-local id and the `existing` lookup does miss across sessions, but the accumulation path is not reached — `PersistTextureAsset` is only entered from `PersistMaterialAsset`. |
| Global reflection texture never unbound; RT destroyed while bound | `Rendering/Water/FcPlanarReflectionRendererFeature.cs:150` | `Dispose` only runs on pipeline teardown, after which the pipeline is immediately reconstructed. Neither concrete failure survives. |
| SSPR samples camera depth/color with SSPR-resolution UVs, ignoring URP RTHandle scaling | `Shaders/Water/FcSsprCompute.compute:45` | Premise wrong for RenderGraph: `_CameraDepthTexture`/`_CameraOpaqueTexture` are created with an exact-size descriptor, not as growing RTHandles. |

Other standing non-findings: the two coordinate spaces (importer `(x,z,-y)` vs scene `(x,z,y)`),
the negative Z scale on brush instances, and the reserved water stencil bit 16 are **intentional
design** documented in CLAUDE.md. Do not file them.
